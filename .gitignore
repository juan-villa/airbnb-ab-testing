# Unzipped raw data — the zip is committed, the CSV is extracted on first run
data/*.csv

# SQLite database — regenerated by `make all`, no need to version 14MB of it
outputs/*.db

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Scratch notebook (local experimentation only)
testing.ipynb

# OS
.DS_Store

# Local tool config
.claude/
